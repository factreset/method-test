containers:
  - name: app
    dockerfile: app/Dockerfile
services:
  - name: web
    container: app
    ports:
      - name: http
        port: 3000   # Change this to your app's port
deploys:
  - name: prod
    services:
      - service: web
        host: localhost
        port: 8080
        target: http